# Enter your API key

Click **Set API Key** above or open the Command Palette and run:

**`CrofAI: Manage CrofAI Provider`**

Paste your key and press Enter.

Your key is stored in VS Code's encrypted `SecretStorage` — it is never written to `settings.json` or any log file.
